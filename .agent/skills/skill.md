# backend
1. backend realted code always in project directory backend

2. always organize the project code in production grade way, schema or db realted files should be always in models folder


# frontend

1. frontend realted code always in project directory frontend
2. never create a page title on each page such as 'seetings' in setting page
3. never run npm build yourself